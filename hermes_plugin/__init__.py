"""Hermes plugin adapters for pagedistiller. Deploy the ``pagedistiller/``
subdirectory (as-is) to ``~/.hermes/plugins/web/pagedistiller/`` -- this
top-level ``__init__.py`` only exists to make the adapter importable from
pagedistiller's own test suite; Hermes's plugin loader never sees it.
"""
